- name: git pull only
  hosts: all
  become: yes
  become_user: mc
  become_method: sudo

  vars:
    repo_dir: "/fake/path"  # remo must be already be cloned

  tasks:
    - name: git pull in repo_dir
      ansible.builtin.command: git pull
      args:
        chdir: "{{ repo_dir }}"
